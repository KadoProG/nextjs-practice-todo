import { LoginForm } from './components/LoginForm';

export const page = () => {
  return (
    <div className="flex h-screen items-center justify-center">
      <LoginForm />
    </div>
  );
};

export default page;
